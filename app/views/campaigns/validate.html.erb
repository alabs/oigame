<% @page_title = t(:token_invalid_title) %>

<%= render :partial => "thanks", :locals => {
  :page_title => @page_title,
  :campaign => @campaign,
  :sub_oigame => @sub_oigame,
  :campaigns => @campaigns,
  :message_type => "error",
  :message => I18n.t(:token_invalid_body)
} %>
